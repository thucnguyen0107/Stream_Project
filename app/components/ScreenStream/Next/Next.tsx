import React from 'react';
import styles from './Next.css';

export default function Next() {
    return(
        <button className={styles.switch_position}>
            SWITCH POSITION
        </button>
    );
}